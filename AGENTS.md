# 项目协作规范

本文件是本工程唯一的代理和贡献者说明，适用于 Codex、Claude、Gemini 以及其他开发工具。不要为某个工具另建一份内容相同的 `AGENT.md`、`CLAUDE.md` 或 `GEMINI.md`；所有项目级规则统一维护在这里。

## 开始和结束工作

- 开始处理任务前先阅读 [`docs/TASKS.md`](docs/TASKS.md)，了解当前进度和已确认范围。
- 玩法和系统规则以 [`docs/DESIGN.md`](docs/DESIGN.md) 为唯一详细来源；术语以 [`docs/GLOSSARY.md`](docs/GLOSSARY.md) 为准。
- 完成涉及当前进度的工作后更新 `docs/TASKS.md`。它只记录仍有价值的任务，不保留历史完成清单。
- 新增或修改系统时，先同步设计文档，再实现代码；不要把同一规则复制到多个文档或工具配置中。
- 修改前先检查工作区现有变更，保留与当前任务无关的用户修改。

## 技术和架构

- 工程使用 Godot 4.7，脚本使用 GDScript，场景使用 `.tscn`。
- 优先使用组合而非深层继承。实体应通过节点组合复用能力，例如视觉节点、生命组件、攻击碰撞组件、受击碰撞组件和状态相关组件。
- 组件通过 signal 向上通知父节点或全局单例；父节点通过方法向下控制子组件。
- 无法通过父子关系表达的跨系统通信使用 `src/autoload/event_bus.gd` 或合适的全局管理器，避免实体之间直接互相持有和调用。
- 保持运行时逻辑、场景资源和测试的职责分离；不要把正式关卡逻辑临时塞进测试场景。

## 目录约定

- `src/autoload/`：全局单例，如事件总线、游戏流程、波次、设置和升级管理器。
- `src/components/`：可复用的生命、碰撞、属性和移动组件。
- `src/entities/`：玩家、敌人、Boss、投射物、掉落物等具体游戏实体。
- `src/effects/`：伤害区、危险区和技能视觉效果。
- `src/levels/`：关卡脚本。
- `src/ui/`：HUD、菜单、升级、调谐和触控界面。
- `src/weapons/`：武器基类和具体武器逻辑。
- `src/tests/`：自动化测试和视觉烟雾测试脚本。
- `scenes/`：场景资源，按 `entities/`、`levels/`、`ui/`、`weapons/`、`world/` 和 `tests/` 分类。
- `assets/textures/`：运行时纹理，按用途分类保存。
- `player_design/`：主角设计素材和生成记录，不作为运行时逻辑目录。
- `docs/`：设计、术语、任务和架构决策；根目录 README 负责项目介绍。

## 命名约定

- `class_name` 使用 PascalCase。
- 文件名使用 snake_case；对应的脚本和场景保持一致的基础名称。
- 变量和方法使用 snake_case。
- 常量使用 CONSTANT_CASE。
- 新文件放入与其职责匹配的既有目录，不在根目录随意添加工具专属说明或重复文档。

## 文档约定

- `README.md` 只负责介绍工程用途、运行方式、目录结构和文档导航。
- `docs/DESIGN.md` 维护完整玩法规则、数值约束和系统边界。
- `docs/GLOSSARY.md` 维护术语定义，其他文档引用术语而不是重新解释一遍。
- `docs/TASKS.md` 维护当前任务状态。
- `docs/ADR-*.md` 记录已经做出的重要架构决策及其取舍。
- 文档发生重命名或移动时，必须同步修正所有引用；不保留旧文件副本作为兼容入口。
